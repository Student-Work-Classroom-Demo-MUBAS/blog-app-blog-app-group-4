<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Blog Posts</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <!----Header-->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/">My Blog</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link active" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/create">Create Post</a>
          </li>
          
          </li>
        </ul>
      </div>
    </div>
  </nav>
  <div class="container py-5">
    <div class="row g-4">
      <% posts.forEach(post => { %>
        <div class="col-12 col-md-6 col-lg-4">
          <div class="card h-60 shadow-sm">
            <img src="<%= post.image %>" class="card-img-top h-40 w-full object-cover" alt="<%= post.title %>" style="height: 220px; object-fit: cover;">
            <div class="card-body d-flex flex-column">
              <h6 class="text-primary mb-2">
                <%= post.author %> · <%= post.date %>
              </h6>
              <h5 class="card-title"><%= post.title %></h5>
              <p class="card-text flex-grow-1"><%= post.description.substring(0, 150) %></p>
              <div class="mb-2">
                <% post.tags.forEach(tag => { %>
                  <span class="badge bg-secondary me-1"><%= tag %></span>
                <% }) %>
              </div>
              <a href="post/<%= post.id %>" class="btn btn-outline-primary mt-auto">Read More →</a>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
  <!-------footer-->
   <footer class="bg-dark text-white text-center py-3 mt-5">
    <p class="mb-0">&copy; <%= new Date().getFullYear() %> My Blog | All Rights Reserved</p>
  </footer>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</body>
</html>
