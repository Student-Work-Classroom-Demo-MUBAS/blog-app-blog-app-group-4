<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Delete Post</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <div class="card shadow p-4">
      <% if (post) { %>
        <h2 class="mb-4 text-danger">Are you sure you want to delete this post?</h2>
        <p><strong>Title:</strong> <%= post.title %></p>
        <p><strong>Author:</strong> <%= post.author %></p>
        <p><strong>Date:</strong> <%= post.date %></p>

        <% if (post.image) { %>
          <img src="<%= post.image %>" class="img-fluid mb-3" style="max-height:200px;">
        <% } %>

        <!-- Delete Form -->
        <form action="/delete/<%= post.id %>" method="POST">
          <button type="submit" class="btn btn-danger">Yes, Delete</button>
          <a href="/post/<%= post.id %>" class="btn btn-secondary">Cancel</a>
        </form>

        <!-- Success Message -->
        <% if (typeof successMessage !== "undefined") { %>
          <div class="alert alert-success mt-3"><%= successMessage %></div>
        <% } %>

      <% } else { %>
        <!-- Error Handling -->
        <div class="alert alert-danger">
          Post not found. <a href="/" class="alert-link">Go back to home</a>.
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>
